/**
 * Renderer-neutral traces for the topology bake-off. Values and names are
 * fictitious, deterministic, and intentionally exercise different graph shapes.
 */

function event(at, type, fields = {}) {
  return { at, type, ...fields };
}

export const traceRendererCases = Object.freeze([
  {
    id: "many-to-many",
    description: "Fan-in, buffering, competing consumers, isolated failure, retry, and recovery.",
    captureFrames: [0, 8, 14, 17, 20, 23],
    trace: {
      v: 1,
      title: "Many-to-many asynchronous messaging",
      actors: [
        { id: "checkout", label: "Checkout API", type: "service" },
        { id: "billing", label: "Billing API", type: "service" },
        { id: "inventory", label: "Inventory API", type: "service" },
        { id: "orders", label: "Topic / queue", type: "queue", capacity: 6 },
        { id: "worker1", label: "Worker 1", type: "pool", capacity: 1 },
        { id: "worker2", label: "Worker 2", type: "pool", capacity: 1 },
        { id: "worker3", label: "Worker 3", type: "pool", capacity: 1 },
        { id: "worker4", label: "Worker 4", type: "pool", capacity: 1 },
      ],
      events: [
        event(0, "send", { from: "checkout", to: "orders", item: "order-1044", caption: "Checkout publishes order-1044" }),
        event(1, "enqueue", { target: "orders", item: "order-1044", caption: "order-1044 is buffered" }),
        event(2, "send", { from: "billing", to: "orders", item: "payment-882", caption: "Billing publishes payment-882" }),
        event(3, "enqueue", { target: "orders", item: "payment-882", caption: "payment-882 is buffered" }),
        event(4, "send", { from: "inventory", to: "orders", item: "stock-517", caption: "Inventory publishes stock-517" }),
        event(5, "enqueue", { target: "orders", item: "stock-517", caption: "stock-517 is buffered" }),
        event(6, "send", { from: "checkout", to: "orders", item: "order-1045", caption: "A burst adds order-1045" }),
        event(7, "enqueue", { target: "orders", item: "order-1045", caption: "Four messages are buffered" }),
        event(8, "dequeue", { from: "orders", to: "worker1", item: "order-1044", caption: "Worker 1 claims order-1044" }),
        event(8, "start", { target: "worker1", item: "order-1044", caption: "Worker 1 processes order-1044" }),
        event(9, "dequeue", { from: "orders", to: "worker2", item: "payment-882", caption: "Worker 2 claims payment-882" }),
        event(9, "start", { target: "worker2", item: "payment-882", caption: "Worker 2 processes payment-882" }),
        event(10, "dequeue", { from: "orders", to: "worker3", item: "stock-517", caption: "Worker 3 claims stock-517" }),
        event(10, "start", { target: "worker3", item: "stock-517", caption: "Three consumers process independently" }),
        event(11, "complete", { target: "worker1", item: "order-1044", caption: "Worker 1 completes order-1044" }),
        event(12, "failure", { target: "worker2", item: "payment-882", caption: "Worker 2 fails without affecting its peers" }),
        event(13, "retry", { from: "worker2", to: "orders", item: "payment-882", caption: "The unacknowledged message returns to the queue" }),
        event(14, "enqueue", { target: "orders", item: "payment-882", caption: "payment-882 waits for another consumer" }),
        event(15, "complete", { target: "worker3", item: "stock-517", caption: "Worker 3 completes stock-517" }),
        event(16, "recovery", { target: "worker2", caption: "Worker 2 recovers" }),
        event(17, "dequeue", { from: "orders", to: "worker1", item: "order-1045", caption: "Worker 1 claims the next message" }),
        event(17, "start", { target: "worker1", item: "order-1045", caption: "Worker 1 processes order-1045" }),
        event(18, "dequeue", { from: "orders", to: "worker3", item: "payment-882", caption: "Worker 3 claims the retried message" }),
        event(18, "start", { target: "worker3", item: "payment-882", caption: "The retry is isolated from producers" }),
        event(20, "complete", { target: "worker1", item: "order-1045", caption: "Worker 1 completes order-1045" }),
        event(21, "complete", { target: "worker3", item: "payment-882", caption: "Worker 3 completes the retried message" }),
      ],
    },
  },
  {
    id: "burst-and-drain",
    description: "Queue growth while arrivals exceed capacity, followed by a complete drain.",
    captureFrames: [0, 9, 17, 25],
    trace: {
      v: 1,
      title: "Burst buffering and queue drain",
      actors: [
        { id: "web", label: "Web orders", type: "service" },
        { id: "imports", label: "Bulk imports", type: "service" },
        { id: "jobs", label: "Order jobs", type: "queue", capacity: 10 },
        { id: "consumer1", label: "Consumer 1", type: "pool", capacity: 1 },
        { id: "consumer2", label: "Consumer 2", type: "pool", capacity: 1 },
        { id: "consumer3", label: "Consumer 3", type: "pool", capacity: 1 },
      ],
      events: [
        event(0, "send", { from: "web", to: "jobs", item: "job-1" }),
        event(0, "enqueue", { target: "jobs", item: "job-1" }),
        event(1, "send", { from: "imports", to: "jobs", item: "job-2" }),
        event(1, "enqueue", { target: "jobs", item: "job-2" }),
        event(2, "send", { from: "web", to: "jobs", item: "job-3" }),
        event(2, "enqueue", { target: "jobs", item: "job-3" }),
        event(3, "send", { from: "imports", to: "jobs", item: "job-4" }),
        event(3, "enqueue", { target: "jobs", item: "job-4" }),
        event(4, "send", { from: "web", to: "jobs", item: "job-5" }),
        event(4, "enqueue", { target: "jobs", item: "job-5", caption: "The arrival burst fills half the queue" }),
        event(5, "dequeue", { from: "jobs", to: "consumer1", item: "job-1" }),
        event(5, "start", { target: "consumer1", item: "job-1" }),
        event(6, "dequeue", { from: "jobs", to: "consumer2", item: "job-2" }),
        event(6, "start", { target: "consumer2", item: "job-2" }),
        event(7, "dequeue", { from: "jobs", to: "consumer3", item: "job-3" }),
        event(7, "start", { target: "consumer3", item: "job-3", caption: "All consumers are busy" }),
        event(9, "complete", { target: "consumer1", item: "job-1" }),
        event(9, "dequeue", { from: "jobs", to: "consumer1", item: "job-4" }),
        event(9, "start", { target: "consumer1", item: "job-4" }),
        event(10, "complete", { target: "consumer2", item: "job-2" }),
        event(10, "dequeue", { from: "jobs", to: "consumer2", item: "job-5" }),
        event(10, "start", { target: "consumer2", item: "job-5", caption: "The queue drains after arrivals stop" }),
        event(11, "complete", { target: "consumer3", item: "job-3" }),
        event(13, "complete", { target: "consumer1", item: "job-4" }),
        event(14, "complete", { target: "consumer2", item: "job-5", caption: "The burst is fully drained" }),
      ],
    },
  },
  {
    id: "infrastructure-failure",
    description: "A request crossing a boundary, timing out, recovering, and completing.",
    captureFrames: [0, 5, 8, 11],
    trace: {
      v: 1,
      title: "Infrastructure timeout and recovery",
      actors: [
        { id: "client", label: "Client", type: "service" },
        { id: "api", label: "Orders API", type: "service" },
        { id: "boundary", label: "Network boundary", type: "network" },
        { id: "broker", label: "Event broker", type: "broker" },
        { id: "cache", label: "Order cache", type: "cache" },
        { id: "database", label: "Orders database", type: "database" },
      ],
      events: [
        event(0, "send", { from: "client", to: "api", item: "request-7", caption: "The client sends request-7" }),
        event(1, "send", { from: "api", to: "boundary", item: "request-7" }),
        event(2, "send", { from: "boundary", to: "broker", item: "request-7", caption: "The request crosses the network boundary" }),
        event(3, "send", { from: "broker", to: "cache", item: "request-7" }),
        event(4, "send", { from: "cache", to: "database", item: "request-7" }),
        event(5, "timeout", { target: "database", item: "request-7", caption: "The database request times out" }),
        event(6, "retry", { from: "cache", to: "database", item: "request-7", caption: "The cache retries the database request" }),
        event(7, "failure", { target: "database", item: "request-7", caption: "The database remains unavailable" }),
        event(8, "recovery", { target: "database", caption: "The database recovers" }),
        event(9, "retry", { from: "cache", to: "database", item: "request-7" }),
        event(10, "complete", { target: "database", item: "request-7" }),
        event(11, "complete", { target: "api", item: "request-7", caption: "The retried request completes" }),
      ],
    },
  },
  {
    id: "dense-topology",
    description: "Thirty actors expose edge congestion, label collision, and responsive limits.",
    captureFrames: [0, 14, 28],
    trace: {
      v: 1,
      title: "Dense service topology",
      actors: [
        ...Array.from({ length: 8 }, (_, index) => ({ id: `source${index + 1}`, label: `Source ${index + 1}`, type: "service" })),
        { id: "gateway", label: "Ingestion gateway", type: "network" },
        { id: "commands", label: "Command broker", type: "broker" },
        { id: "events", label: "Event queue", type: "queue", capacity: 20 },
        ...Array.from({ length: 10 }, (_, index) => ({ id: `worker${index + 1}`, label: `Worker ${index + 1}`, type: "pool", capacity: 1 })),
        ...Array.from({ length: 9 }, (_, index) => ({ id: `store${index + 1}`, label: `Store ${index + 1}`, type: index % 2 ? "cache" : "database" })),
      ],
      events: [
        ...Array.from({ length: 8 }, (_, index) => event(index, "send", { from: `source${index + 1}`, to: "gateway", item: `item-${index + 1}` })),
        event(8, "send", { from: "gateway", to: "commands", item: "batch-1" }),
        event(9, "send", { from: "commands", to: "events", item: "batch-1" }),
        ...Array.from({ length: 10 }, (_, index) => event(10 + index, "dequeue", { from: "events", to: `worker${index + 1}`, item: `item-${index + 1}` })),
        ...Array.from({ length: 9 }, (_, index) => event(20 + index, "send", { from: `worker${index + 1}`, to: `store${index + 1}`, item: `item-${index + 1}` })),
      ],
    },
  },
]);

export const traceRendererViewports = Object.freeze([
  { id: "card", width: 380, height: 420 },
  { id: "fullscreen", width: 1280, height: 760 },
]);
